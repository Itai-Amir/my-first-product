feature_id: F-0100
title: Hello World CLI

description: >
  Implement a minimal command-line interface that prints
  "Hello, World!" when executed.

inputs:
  - command line execution

outputs:
  - stdout text "Hello, World!"

assumptions:
  - Python is used
  - No external dependencies are required

acceptance_criteria:
  - Running `python hello.py` prints exactly "Hello, World!"
